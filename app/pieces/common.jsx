import {
	state,
	react,
	construct,
	react,
	destruct,
	write,
	route,
	page,
	timer,
	req,
	D,
	T,
	B,
	P,
	I,
	V,
} from "~/config/shop"

export default () => {
	var nav = route()

	construct(() => {
		page.title = ``
	})

	return <D style={`fit_1`}></D>
}
